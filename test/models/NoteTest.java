package models;

import static org.junit.Assert.*;

import org.junit.Test;

import util.BaseModelTest;

public class NoteTest extends BaseModelTest {

   /* @Test 
    public void NoteDBTest() {
    	Note note = new Note("new note", new User("alice@gmx.de", "Alice", "geheimnis", null, null, null, null, null, null, null));
        Note.create(note);
        assertNotNull(note.id);
        assertTrue(Note.all().size() >= 1);
        Note.delete(note.id);
        assertTrue(Note.all().size() == 0);
    }*/

}